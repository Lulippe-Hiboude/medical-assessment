spring:
  data:
    mongodb:
      uri: mongodb://${MONGO_INITDB_ROOT_USERNAME}:${MONGO_INITDB_ROOT_PASSWORD}@localhost:27017/notedb-test?authSource=admin
      database: notedb-test

jwt:
  secret: ${JWT_SECRET}
  expiration: 3600000